//  Projects types
export type EventProjects = {
    title: string;
    image: string;
  };

  // Services types
  
  export type EventServices = {
  title: string;
  description: string;
  image: string;
};